#include "main.h"
#include <stdlib.h>
/**
 * _strcpy - a function that copies the files
 * @dest: this is where to cop the string
 * @src: A pointer to source string to copy from
 * Return: pointer to destination
 */
char *_strcpy(char *dest, char *src)
{
	strcpy(dest, src);
	return (dest);
}
